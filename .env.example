# No secrets required for this portfolio.
# GitHub projects are fetched server-side without authentication.
# Contact form uses Formspree (endpoint is hardcoded in ContactSection.tsx).
#
# If you fork this and want to add private GitHub API access:
# GITHUB_TOKEN=your_github_personal_access_token
